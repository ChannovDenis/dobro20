# PRODUCT_SCOPE.md — Полный аудит приложения «Добросервис»

> Дата аудита: 15 февраля 2026  
> Версия: 1.0.0

---

## 1. Общее описание

**Добросервис** — мобильное супер-приложение (PWA, mobile-first) класса B2B2C/B2B2E, предоставляющее пользователям доступ к AI-ассистентам и живым экспертам по 12 направлениям: юрист, врач, психолог, стилист, финансист, садовод, ветеринар, антимошенник, ЗОЖ-тренер, риэлтор, косметолог, универсальный ассистент.

Платформа построена как **white-label решение** с мульти-тенантной архитектурой. Поддерживаемые тенанты: Добросервис (default), Газпромбанк (gpb), Wildberries (wb). Тенант определяется через URL-параметр `?tenant=slug` или поддомен.

### Целевые роли пользователей

| Роль | Описание | Реализация |
|------|----------|------------|
| **Конечный пользователь** | Задаёт вопросы AI, записывается к экспертам | ✅ Полностью |
| **Партнёр-администратор** | Просматривает дашборд активности | ⚠️ Моковые данные |
| **Супер-администратор** | Управление тенантами и пользователями | ❌ Не реализовано |

### Бизнес-модель

- **Подписка «Премиум»** — заявлена в UI (бейдж в профиле, промо-карточка), но **не реализована** (нет платежей, нет ограничений по тарифу)
- **Реферальная программа** — промо-карточка с кнопкой «Поделиться» (только Web Share API / копирование ссылки)
- **Платные консультации** — цены у экспертов отображаются, но оплата не подключена

---

## 2. Карта экранов

### 2.1 Splash Screen

| | |
|---|---|
| **Роут** | `/splash` |
| **Данные** | Статика |
| **Описание** | Анимированный сплеш-скрин с логотипом (иконка Bot), названием «ДОБРОСЕРВИС», подзаголовком и тремя пульсирующими точками. Автоматический редирект на `/feed` через 2 секунды. |
| **Кликабельные элементы** | Нет |
| **Состояния** | Единственное (анимация → редирект) |

---

### 2.2 Feed (TikTok-лента)

| | |
|---|---|
| **Роут** | `/feed` |
| **Данные** | Моки (`feedItems` — 15 карточек в `mockData.ts`) |
| **Описание** | Вертикальная лента в стиле TikTok с snap-scroll. Полноэкранные карточки с фоновым изображением, заголовком, описанием, тегами, автором, кнопками лайков/комментариев/шеров. Некоторые карточки содержат видео. |
| **Типы карточек** | `content` (12 шт.), `service-promo` (2 шт.), `miniapp-promo` (1 шт.) |
| **Кликабельные элементы** | Лайк, Комментарий, Поделиться (toast-заглушки), CTA на промо-карточках (переход в чат/сервис/мини-приложение) |
| **Нижняя навигация** | 3 таба: Лента, Чат (приподнятая кнопка с градиентом), Сервисы |
| **Состояния** | Только с данными (нет пустого/загрузочного) |

---

### 2.3 Chat (AI-чат)

| | |
|---|---|
| **Роут** | `/chat` |
| **Параметры** | `?prompt=...&context=...&service=...&topicId=...` |
| **Данные** | База данных (`topics`, `topic_messages`) + Edge Functions |
| **Описание** | Полноэкранный модальный чат с AI-ассистентом. Стриминг ответов через SSE. Поддержка 9 специализированных ассистентов с уникальными персонами. |
| **UI-элементы** | Хедер (кнопка X, иконка ассистента, имя, кнопка эксперта), контекст-бар топика (при наличии), область сообщений, тикер подсказок (пустое состояние), поле ввода с кнопками фото/отправки |
| **Кликабельные элементы** | X (закрыть → /feed), Кнопка эксперта (переход в /service/:id), Подсказки-чипсы (автозаполнение промпта), Кнопки действий в ответах (примерка, цветотип, тренды и др.), Фото-загрузка, Меню топика (переименование, очистка, архив, удаление) |
| **Состояния** | Пустой (тикер подсказок), Загрузка истории (спиннер), С сообщениями, Загрузка ответа (пульсирующие точки), Контекстный бейдж (при переходе из ленты) |
| **Edge Functions** | `lisa-stylist` (основной чат), `colortype-analyzer`, `virtual-tryon` |

---

### 2.4 Services (Хаб сервисов)

| | |
|---|---|
| **Роут** | `/services` |
| **Данные** | Моки (`services`, `promotions`, `quickActions`, `userProfile` из `mockData.ts`) |
| **Описание** | Полноэкранная модалка — хаб всех сервисов. Профиль пользователя в хедере, промо-карусель, быстрые действия, AI-ассистент промо, история консультаций, категоризированные сервисы, статистика. |
| **Секции** | Промо-карусель (Премиум, Реферал), Quick Actions (Сканер, Оплата, Кэшбэк, Бонусы — все toast-заглушки), AI-ассистент (→ /chat), История (→ /history), 4 категории сервисов (Сезонное, Популярное, Финансы, Образ жизни), Статистика (захардкожено: 12 консультаций, ₽2340, 47 дней) |
| **Кликабельные элементы** | X (→ /feed), Аватар/настройки (→ /settings), Колокольчик (toast), Промо-кнопки (toast/share), Quick Actions (toast), AI-ассистент (→ /chat), История (→ /history), Карточки сервисов (→ /service/:id), «Все» по категориям (toast) |
| **Состояния** | Только с данными |

---

### 2.5 Settings (Настройки)

| | |
|---|---|
| **Роут** | `/settings` |
| **Данные** | Моки (`userProfile`) + Auth (для кнопки выхода) |
| **Описание** | Профиль пользователя (захардкожено «Денис Чаннов», Премиум), сетка сервисов, секции настроек (подписка, уведомления, безопасность, тема, партнёрам, приложение, о приложении), соцсети, кнопка выхода. |
| **Кликабельные элементы** | Аватар (edit — нет действия), Сервисы-сетка (→ /chat?service=...), Переключатели уведомлений (только локальный state), Панель партнёра (→ /admin), ThemeSwitcher (режим: Light/Dark/Auto + акцентные цвета тенантов), Выйти/Войти (Auth) |
| **Тема** | ThemeSwitcher переключает класс `light`/`dark` на `<html>` + `localStorage`. Акцентные кнопки устанавливают `data-tenant` и перезаписывают CSS-переменные |
| **Состояния** | Авторизован (кнопка «Выйти»), Неавторизован (кнопка «Войти») |

---

### 2.6 Auth (Вход/Регистрация)

| | |
|---|---|
| **Роут** | `/auth` |
| **Данные** | База данных (Supabase Auth) |
| **Описание** | Форма входа/регистрации с email и паролем. Логотип, поля ввода с иконками, кнопка показа пароля, переключатель режимов. |
| **Валидация** | Zod: email — `z.string().email()`, пароль — `z.string().min(6)` |
| **Кликабельные элементы** | Назад (navigate(-1)), Войти/Зарегистрироваться, Показать/скрыть пароль, Переключить режим |
| **Состояния** | Форма входа, Форма регистрации, Загрузка (спиннер), Ошибки валидации (красный текст), Авторизован (редирект на /feed) |

---

### 2.7 ServiceDetail (Детали сервиса)

| | |
|---|---|
| **Роут** | `/service/:id` |
| **Данные** | Моки (`services`, `experts` из `mockData.ts`) |
| **Описание** | Hero-баннер сервиса с иконкой и описанием, фильтры (Все/Онлайн/Топ), список экспертов с аватаром, рейтингом, специализацией, ценой, онлайн-статусом, кнопками «Записаться» и «Сейчас». |
| **Данные экспертов** | 4 категории с экспертами: garden (3), lawyer (3), doctor (2), psychologist (1). Итого 9 экспертов. |
| **Кликабельные элементы** | Назад, Фильтры, Карточки экспертов (→ /service/:id/expert/:expertId), Кнопки «Записаться»/«Сейчас», «Открыть AI-чат» (если нет экспертов) |
| **Состояния** | С экспертами, Без экспертов (CTA в AI-чат), Сервис не найден |

---

### 2.8 ExpertDetail (Запись к эксперту)

| | |
|---|---|
| **Роут** | `/service/:id/expert/:expertId` |
| **Данные** | Моки (`experts`) + localStorage (бронирования) |
| **Описание** | Профиль эксперта (аватар, рейтинг, биография, статистика), кнопки связи (Phone, Video, Chat, File — без действий), выбор даты (7 дней вперёд), выбор времени (слоты из мока), тип консультации (онлайн/чат), CTA «Записаться». |
| **Кликабельные элементы** | Назад, Даты, Время-слоты, Тип (онлайн/чат), Phone/Video/Chat/File (нет действий), «Записаться» (сохраняет в localStorage → toast → navigate(/history)) |
| **Состояния** | Эксперт найден (с/без свободных слотов), Эксперт не найден |
| **⚠️ Ограничение** | Бронирование сохраняется в `localStorage`, а не в БД |

---

### 2.9 History (Мои консультации)

| | |
|---|---|
| **Роут** | `/history` |
| **Данные** | Моки + localStorage |
| **Описание** | Список консультаций (с экспертами и AI). Фильтр (Все/Предстоящие/Завершённые/AI). Карточки с датой, экспертом, типом, ценой, статусом. |
| **Моковые данные** | 3 демо-бронирования + 2 демо AI-консультации (если нет реальных в localStorage) |
| **Кликабельные элементы** | X (→ /services), Фильтр (Select), Отменить (обновляет localStorage), Перенести (toast-заглушка), Войти/Открыть (для онлайн — toast «скоро»; для чата — → /chat) |
| **Состояния** | С данными, Пустое (CTA «Найти эксперта» → /services) |

---

### 2.10 MiniApp (Мини-приложение)

| | |
|---|---|
| **Роут** | `/mini-app/:id` |
| **Данные** | Моки (`miniApps` — 4 заглушки) |
| **Описание** | Лендинг мини-приложения с иконкой, названием, описанием, лейблом «Powered by AI», CTA «Открыть в чате» (→ /chat), список фич (захардкожено по ID). |
| **4 мини-приложения** | Счётчик калорий, Проверка документов, Планировщик бюджета, Проверка симптомов |
| **Кликабельные элементы** | Назад, «Открыть в чате» (→ /chat), «Назад» |
| **Состояния** | Найдено, Не найдено |
| **⚠️ Ограничение** | Это только лендинги, реального функционала мини-приложений нет |

---

### 2.11 Admin (Панель партнёра)

| | |
|---|---|
| **Роут** | `/admin` |
| **Данные** | Моки (все метрики) + RLS-проверка ролей (`user_roles`) |
| **Описание** | Дашборд партнёра с метриками, графиком активности за неделю (Recharts), топ сервисов, последние эскалации. |
| **Доступ** | Проверка `has_role('partner_admin')` или `has_role('super_admin')` через DB-функцию |
| **Кликабельные элементы** | Назад (navigate(-1)) |
| **Состояния** | Загрузка (спиннер), Не авторизован (редирект на /), Нет доступа (экран «Доступ запрещён»), Ошибка, Дашборд с данными |
| **⚠️ Ограничение** | Все метрики захардкожены, нет реальной аналитики |

---

### 2.12 Index (Точка входа)

| | |
|---|---|
| **Роут** | `/` |
| **Действие** | `<Navigate to="/splash" replace />` |

---

### 2.13 NotFound

| | |
|---|---|
| **Роут** | `*` |
| **Описание** | Статическая страница 404 |

---

## 3. Навигация

### Структура навигации

```
BottomNav (фиксированная, 3 таба):
├── 🏠 Лента     → /feed
├── 💬 Чат       → /chat (приподнятая кнопка с градиентом)
└── ⊞  Сервисы   → /services
```

### Схема переходов

```
/splash (2 сек) → /feed
                    ├── BottomNav → /chat, /services
                    └── Карточки ленты → /chat?prompt=...&context=...

/services (модалка)
    ├── X → /feed
    ├── ⚙️ → /settings
    ├── 🕐 → /history
    ├── 🤖 → /chat
    ├── Сервис → /service/:id
    │              └── Эксперт → /service/:id/expert/:expertId
    │                              └── Записаться → /history
    └── Промо → toast

/settings
    ├── Сервисы → /chat?service=...
    ├── Партнёрам → /admin
    └── Войти/Выйти → /auth

/chat (модалка)
    ├── X → /feed
    └── 👤 Эксперт → /service/:id

/auth → (после входа) /feed
```

### Точки входа

1. **Основная**: `/` → `/splash` → `/feed`
2. **Прямая ссылка**: Любой роут доступен напрямую
3. **Тенант**: `?tenant=gpb` или `gpb.dobroservis.ru`
4. **Контекстный чат из ленты**: `/chat?prompt=...&context=...`
5. **Сервисный чат**: `/chat?service=lawyer`

---

## 4. Реализованная функциональность

### ✅ Работает полностью

#### Авторизация
- Email/password через Supabase Auth
- Вход (`signIn`) и регистрация (`signUp`)
- Подтверждение email при регистрации
- Zod-валидация полей (email формат, пароль ≥6 символов)
- Редирект авторизованного на /feed
- Выход из аккаунта (signOut)
- Кнопка показа/скрытия пароля

#### AI-чат
- Стриминговые ответы через SSE (Server-Sent Events)
- Edge Function `lisa-stylist` — основной чат через Google Gemini
- 9 специализированных AI-ассистентов с уникальными персонами:
  - Юрист Алексей, Доктор Мария, Психолог Анна, Тренер Дима, Финансист Олег, Садовод Нина, Стилист Лиза, Ветеринар Макс, Эксперт по безопасности
- Каждый ассистент имеет свой цвет, иконку и набор подсказок (по 6 шт.)
- Универсальный «Добро-ассистент» с 18 подсказками
- Персистентные топики и сообщения в БД
- Автосоздание топика при первом сообщении
- Загрузка истории сообщений при выборе топика
- Управление топиками: переименование, очистка сообщений, архивация, удаление
- Контекстные кнопки действий в ответах (примерка, цветотип, тренды, стиль)

#### Анонимный режим
- `session_id` генерируется в `lib/session.ts` и хранится в `localStorage`
- Передаётся в заголовке `x-session-id` через кастомный Supabase-клиент
- RLS-политики проверяют `session_id` для анонимных пользователей

#### TikTok-style лента
- Вертикальный snap-scroll
- Полноэкранные карточки с фоновыми изображениями
- Видео-контент (autoplay при видимости)
- Промо-карточки сервисов и мини-приложений

#### Темизация
- Тёмная тема (default) + Светлая тема + Auto-режим
- Переключение через ThemeSwitcher в настройках
- Сохранение выбора в `localStorage`
- Мульти-тенантные цветовые схемы: Добросервис (зелёный), Газпромбанк (синий), Wildberries (фиолетовый)
- Акцентный цвет тенанта переопределяет `--primary`, `--ring`, `--glow-primary`

#### Мульти-тенантность
- Определение тенанта через URL-параметр или поддомен
- Загрузка конфигурации тенанта из `public_tenant_info` (view)
- Применение CSS-переменных динамически

#### RLS-политики
- `topics`: select/insert/update по `user_id` или `session_id`
- `topic_messages`: select/insert по связи через topic
- `profiles`: select/insert/update по `user_id`
- `user_roles`: select по `user_id`
- `analytics_events`: insert для всех

#### Аналитика
- Таблица `analytics_events` с event_type, event_data, session_id, user_id, tenant_id
- Хук `useAnalytics.ts` для записи событий

#### Эскалация к эксперту
- После 5 сообщений в чате появляется карточка эскалации
- Карточка предлагает перейти к живому эксперту соответствующего сервиса

#### Дополнительные Edge Functions
- `colortype-analyzer` — анализ цветотипа по фото
- `virtual-tryon` — виртуальная примерка одежды
- `chat` — альтернативный endpoint чата

---

### ⚠️ Частично реализовано / заглушки

| Функция | Статус | Детали |
|---------|--------|--------|
| Голосовые сообщения | 🟡 Заглушка | `useVoiceRecorder.ts` — только таймер, `console.log` при остановке |
| Бронирование экспертов | 🟡 localStorage | Сохраняется в `localStorage`, не в БД |
| Уведомления | 🟡 Toast | Колокольчик → toast «нет уведомлений», переключатели — только локальный state |
| Quick Actions | 🟡 Toast | Сканер, Оплата, Кэшбэк, Бонусы — все показывают toast «скоро» |
| Видеозвонки | 🟡 Toast | Кнопка «Войти» → toast «скоро» |
| Перенос записи | 🟡 Toast | Кнопка «Перенести» → toast «скоро» |
| Промо-карточки | 🟡 Toast | Премиум → toast «активирован», Реферал → Web Share / копирование |
| Профиль | 🟡 Хардкод | `ProfileHeader` использует `userProfile` из mockData (Денис Чаннов) |
| Телефон/видео/файл эксперта | 🟡 Нет действий | Кнопки Phone/Video/Chat/File в ExpertDetail ничего не делают |
| Категории «Все» | 🟡 Toast | Кнопка «Все» → toast «раздел скоро» |

---

## 5. Мок-данные vs реальная логика

### На моках (захардкожено в `src/data/mockData.ts`)

| Данные | Объём | Использование |
|--------|-------|---------------|
| `feedItems` | 15 карточек | TikTokFeed |
| `services` | 12 сервисов | Services, ServiceDetail, SuperAppGrid |
| `experts` | 9 экспертов (4 категории) | ServiceDetail, ExpertDetail |
| `userProfile` | 1 объект | ProfileHeader, Services header |
| `miniApps` | 4 заглушки | MiniApp |
| `contentItems` | 6 элементов | ContentFeed |
| `chatTemplates` | 6 шаблонов | (не используется в текущем UI) |
| `quickActions` | 4 действия | (не используется — другие quickActions в Services) |
| `superAppItems` | 12 элементов | SuperAppGrid в Settings |
| `contentCategories` | 6 категорий | (доступно, не используется) |
| AI Assistants | 11 конфигов | `aiAssistants.ts` — все подсказки, цвета, иконки |
| Промо / Quick Actions | 2 + 4 | Services (inline в компоненте) |
| Статистика | 3 числа | Services footer (12/₽2340/47) |
| Admin метрики | Все | MetricsGrid, ActivityChart, TopServicesTable, RecentEscalations |
| Booking demos | 3 + 2 | History (getMockBookings, getMockAIConsultations) |

### Подключено к реальному бэкенду (Lovable Cloud)

| Ресурс | Тип | Операции |
|--------|-----|----------|
| `auth.users` | Auth | signup, signin, signout, getUser |
| `topics` | Table | SELECT, INSERT, UPDATE, DELETE |
| `topic_messages` | Table | SELECT, INSERT |
| `profiles` | Table | SELECT, INSERT, UPDATE |
| `analytics_events` | Table | INSERT |
| `user_roles` | Table | SELECT (через `has_role` функцию) |
| `public_tenant_info` | View | SELECT (публичная) |
| `tenants` | Table | (через view) |

### Edge Functions (4 штуки)

| Функция | URL | Назначение |
|---------|-----|-----------|
| `lisa-stylist` | `/functions/v1/lisa-stylist` | Основной AI-чат (Gemini, стриминг SSE) |
| `colortype-analyzer` | `/functions/v1/colortype-analyzer` | Анализ цветотипа по фото |
| `virtual-tryon` | `/functions/v1/virtual-tryon` | Виртуальная примерка |
| `chat` | `/functions/v1/chat` | Альтернативный чат-endpoint |

### Database Functions

| Функция | Аргументы | Возвращает |
|---------|-----------|-----------|
| `has_role` | `_role`, `_user_id` | `boolean` |
| `get_user_tenant` | `_user_id` | `string` |
| `user_belongs_to_tenant` | `_tenant_id`, `_user_id` | `boolean` |

---

## 6. Дизайн-система

### Цветовая палитра

**Основные токены (тёмная тема):**

| Токен | Значение (HSL) | Назначение |
|-------|----------------|-----------|
| `--background` | 222 47% 11% | Фон приложения |
| `--foreground` | 210 40% 98% | Основной текст |
| `--card` | 222 47% 14% | Фон карточек |
| `--primary` | 199 89% 48% | Акцент (переопределяется тенантом) |
| `--muted-foreground` | 215 20% 65% | Вторичный текст |
| `--destructive` | 0 84% 60% | Ошибки, удаление |

**Категорийные цвета (12):**

| Категория | HSL | Визуально |
|-----------|-----|-----------|
| legal | 199 89% 48% | Синий |
| health | 142 76% 36% | Зелёный |
| psychology | 280 65% 60% | Фиолетовый |
| finance | 38 92% 50% | Оранжевый |
| wellness | 350 89% 60% | Розовый |
| security | 262 83% 58% | Индиго |
| vet | 25 95% 53% | Оранж |
| style | 328 86% 70% | Розовый яркий |
| assistant | 199 89% 48% | Синий |
| garden | 142 71% 45% | Зелёный |
| realtor | 210 80% 55% | Голубой |
| beauty | 340 82% 65% | Малиновый |

**Тенант-специфичные цвета:**

| Тенант | `--primary` |
|--------|-------------|
| Добросервис | 142 76% 36% (зелёный) |
| Газпромбанк | 210 100% 35% (синий) |
| Wildberries | 280 80% 50% (фиолетовый) |

### Типографика

- **Основной шрифт**: Inter (через system-ui fallback)
- Загружается как системный шрифт, без @font-face
- Размеры: `text-xs` (12px), `text-sm` (14px), `text-base` (16px), `text-lg` (18px), `text-xl` (20px), `text-2xl` (24px), `text-3xl` (30px)

### Эффекты

- **Glassmorphism**: `.glass` / `.glass-card` — `backdrop-blur(20px)` + полупрозрачный фон + border
- **Glow**: `.glow` — `box-shadow` с `--glow-primary`
- **Glow Text**: `.glow-text` — `text-shadow`
- **Gradient Primary**: `.gradient-primary` — линейный градиент 135° от primary до purple

### UI-компоненты (shadcn/ui)

Полный набор из 50+ компонентов, включая: Accordion, AlertDialog, Avatar, Badge, Button, Calendar, Card, Carousel, Checkbox, Command, Dialog, Drawer, DropdownMenu, Form, HoverCard, Input, Label, Menubar, NavigationMenu, Pagination, Popover, Progress, RadioGroup, ScrollArea, Select, Separator, Sheet, Sidebar, Skeleton, Slider, Switch, Table, Tabs, Textarea, Toast, Toggle, Tooltip.

### Адаптивность

- **Mobile-first** — оптимизировано для мобильных устройств
- `safe-top` / `safe-bottom` — поддержка безопасных зон (notch, home indicator)
- **Нет десктоп-версии** — все экраны рассчитаны на ≤430px

---

## 7. Технический стек

### Фреймворк и библиотеки

| Категория | Технология | Версия |
|-----------|-----------|--------|
| UI Framework | React | 18.3.1 |
| Language | TypeScript | — |
| Build Tool | Vite | — |
| Styling | Tailwind CSS + tailwindcss-animate | — |
| UI Kit | shadcn/ui (Radix primitives) | — |
| Animations | Framer Motion | 12.33.0 |
| Routing | React Router DOM | 6.30.1 |
| State | TanStack React Query | 5.83.0 |
| Backend | Supabase JS | 2.95.3 |
| Forms | React Hook Form + Zod | 7.61.1 / 3.25.76 |
| Dates | date-fns | 3.6.0 |
| Charts | Recharts | 2.15.4 |
| Markdown | react-markdown | 10.1.0 |
| Carousel | embla-carousel-react | 8.6.0 |
| Toasts | Sonner | 1.7.4 |
| Drawer | Vaul | 0.9.9 |

### Структура файлов

```
src/
├── components/
│   ├── admin/          # MetricsGrid, ActivityChart, TopServicesTable, RecentEscalations
│   ├── chat/           # ChatMessage, ChatInput, SuggestionTicker, TopicContextBar, etc. (15 компонентов)
│   ├── feed/           # TikTokFeed, FeedCard, ContentFeed, etc. (7 компонентов)
│   ├── layout/         # BottomNav, TopBar, SearchBar
│   ├── settings/       # ProfileHeader, ThemeSwitcher, SettingsSection, SuperAppGrid
│   └── ui/             # 50+ shadcn/ui компонентов
├── constants/          # aiAssistants, chatActions, clothing, trends
├── contexts/           # TenantContext
├── data/               # mockData.ts (471 строк)
├── hooks/              # useAuth, useChat, useTopics, useTenant, useProfile, useAnalytics, useAdminAuth, useVoiceRecorder, use-mobile
├── integrations/supabase/ # client.ts, types.ts (авто-генерация)
├── lib/                # utils.ts, session.ts, supabaseWithSession.ts
├── pages/              # 12 страниц
├── types/              # booking.ts, chat.ts
├── test/               # setup.ts, example.test.ts
└── main.tsx

supabase/
├── config.toml
└── functions/
    ├── chat/
    ├── colortype-analyzer/
    ├── lisa-stylist/
    └── virtual-tryon/
```

### Роутинг

| Роут | Компонент | Описание |
|------|-----------|----------|
| `/` | `<Navigate to="/splash">` | Редирект |
| `/splash` | SplashScreen | Заставка (2 сек) |
| `/feed` | Feed | TikTok-лента |
| `/chat` | Chat | AI-чат (модалка) |
| `/services` | Services | Хаб сервисов (модалка) |
| `/settings` | Settings | Настройки |
| `/service/:id` | ServiceDetail | Детали сервиса |
| `/service/:id/expert/:expertId` | ExpertDetail | Запись к эксперту |
| `/history` | History | История консультаций |
| `/mini-app/:id` | MiniApp | Мини-приложение |
| `/admin` | Admin | Панель партнёра |
| `/auth` | Auth | Вход/Регистрация |
| `*` | NotFound | 404 |

### Стейт-менеджмент

| Тип | Инструмент | Данные |
|-----|-----------|--------|
| Серверный | TanStack React Query | Tenant info |
| Компонентный | React useState | Формы, UI-состояния, локальные данные |
| Контекст | React Context | TenantContext (tenant, theme, slug) |
| Персистентный | localStorage | theme, session_id, bookings |
| Серверный (direct) | Supabase SDK | topics, messages, profiles, auth |

---

## 8. Известные ограничения и баги

### 🔴 Критичное

| # | Проблема | Где | Влияние |
|---|---------|-----|---------|
| 1 | **Нет RLS-policy на DELETE для `topic_messages`** | БД | Очистка сообщений (clearMessages) **молча фейлит** — RLS блокирует операцию |
| 2 | **Нет экрана «Мои чаты»** | UI | Пользователь не видит список своих топиков и не может вернуться в старый чат (только через прямую ссылку с topicId) |

### 🟡 Значительное

| # | Проблема | Где | Детали |
|---|---------|-----|--------|
| 3 | Нет пагинации сообщений | `useChat.ts:75-78` | Загружаются **все** сообщения топика. При длинных диалогах — проблемы с производительностью |
| 4 | `ProfileHeader` хардкод | `ProfileHeader.tsx:4` | Всегда показывает «Денис Чаннов» из `mockData.ts`, игнорируя реальные данные профиля из БД |
| 5 | Бронирования в localStorage | `ExpertDetail.tsx:69` | `saveBooking()` пишет в localStorage. Данные теряются при очистке, не синхронизируются между устройствами |
| 6 | `useVoiceRecorder` — заглушка | `useVoiceRecorder.ts:31` | Только таймер + `console.log`. Нет записи аудио, нет интеграции с чатом |
| 7 | Нет CSS-transition при смене темы | `index.css` | Переключение Light/Dark происходит мгновенно, без плавного перехода |

### 🟢 Минорное / Будущие фичи

| # | Проблема | Детали |
|---|---------|--------|
| 8 | Quick Actions — заглушки | Сканер, Оплата, Кэшбэк, Бонусы — только toast |
| 9 | Видеозвонки — заглушка | «Войти» → toast «скоро» |
| 10 | Перенос записей — заглушка | «Перенести» → toast «скоро» |
| 11 | Мини-приложения — лендинги | 4 MiniApp — только описание фич, нет функционала |
| 12 | Уведомления — заглушка | Только toast, нет push-уведомлений и центра уведомлений |
| 13 | Подписка/платежи — не реализованы | Бейдж «Премиум» есть, но нет логики тарифов и оплаты |
| 14 | Поиск — отсутствует | SearchBar компонент существует, но не интегрирован |
| 15 | Phone/Video/Chat/File кнопки в ExpertDetail | Кнопки не имеют обработчиков |
| 16 | Админ-метрики — все моковые | MetricsGrid, ActivityChart и пр. — захардкожены |
| 17 | Лайки/комментарии/шеры в ленте | Только отображение, нет реальной логики |
| 18 | Статистика «Твоя активность» | Захардкожено: 12 консультаций, ₽2340, 47 дней |
| 19 | Edit-кнопка на аватаре | Кнопка карандаша в ProfileHeader не имеет обработчика |
| 20 | Соцсети в Settings | Instagram/Telegram кнопки без обработчиков |

---

## Приложение: Enums базы данных

```typescript
app_role: "user" | "partner_admin" | "super_admin"
message_role: "user" | "assistant" | "system" | "expert"
topic_status: "active" | "archived" | "escalated"
```
