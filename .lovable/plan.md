
# План доработки клиентского приложения Добросервис

## Обзор задач

1. ~~Перевести 404 на русский~~ - **Уже выполнено!** (файл NotFound.tsx уже на русском)
2. Чат - Универсальный режим (разные AI-ассистенты для разных сервисов)
3. Запись к эксперту - заменить alert() на toast + localStorage
4. Добавить /history (История консультаций) с 4-м табом в навигации
5. Исправить навигацию (кнопка X должна возвращать назад)

---

## Задача 1: Перевод 404 - ПРОПУСКАЕМ

NotFound.tsx уже переведён на русский:
- "Страница не найдена" 
- "Вернуться на главную"

---

## Задача 2: Универсальный чат

### Создать конфигурацию AI-ассистентов

Новый файл `src/constants/aiAssistants.ts`:

| Service ID | Имя | Иконка | Цвет | Подсказки |
|------------|-----|--------|------|-----------|
| lawyer | Юрист Алексей | Scale | 199 89% 48% | "Как оформить развод", "Алименты на ребёнка"... |
| doctor | Доктор Мария | Heart | 350 89% 60% | "Болит голова третий день", "Расшифровка анализов"... |
| psychologist | Психолог Анна | Brain | 280 65% 60% | "Тревожность на работе", "Не могу уснуть"... |
| fitness | Тренер Дима | Dumbbell | 38 92% 50% | "Программа для новичка", "Болит спина после тренировки"... |
| finance | Финансист Олег | Wallet | 142 76% 36% | "Налоговый вычет", "Куда вложить 100 тысяч"... |
| garden | Садовод Нина | Sprout | 84 70% 45% | "Когда сажать рассаду", "Чем подкормить томаты"... |
| style | Стилист Лиза | Sparkles | 328 86% 70% | текущие подсказки |
| vet | Ветеринар Макс | PawPrint | 174 60% 41% | "Кот не ест второй день", "Прививки щенку"... |
| default | Добро-ассистент | Bot | primary | текущие универсальные подсказки |

### Изменения в файлах

**Chat.tsx:**
- Читать `?service=` из URL params
- Получать конфиг ассистента по service ID
- Отображать иконку и имя ассистента в header
- Передавать service в SuggestionTicker

**SuggestionTicker.tsx:**
- Принимать prop `serviceId?: string`
- Фильтровать подсказки по сервису
- Если сервис указан - показывать специфичные подсказки
- Если нет - показывать универсальные

**ServiceDetail.tsx:**
- Кнопка "Открыть AI-чат" → navigate(`/chat?service=${id}`)

**useChat.ts:**
- Передавать service_id в запрос к edge function

---

## Задача 3: Запись к эксперту

### Изменения в ExpertDetail.tsx

**handleBooking() - заменить alert:**

```text
1. Сформировать объект записи:
   {
     id: crypto.randomUUID(),
     expertId, expertName, specialty,
     serviceId, date, time, type, price,
     status: "upcoming",
     createdAt: new Date().toISOString()
   }

2. Сохранить в localStorage["dobro-bookings"]:
   - Прочитать существующий массив
   - Добавить новую запись
   - Записать обратно

3. Показать toast.success:
   "Записано! Консультация {дата} в {время}"

4. navigate('/services')
```

---

## Задача 4: Страница /history

### Новые файлы

**src/pages/History.tsx:**
- Header: "Мои консультации" + Select фильтр (Все/Предстоящие/Прошедшие)
- Mock-данные: 4 карточки консультаций + 1 AI-консультация
- Интеграция с localStorage["dobro-bookings"]

**Структура карточки:**

```text
┌─────────────────────────────────────────────┐
│ 🟢 12 фев, 14:00          [Предстоящая]     │
│ 👩‍⚕️ Мария Сидорова · Врач · Терапевт       │
│ Онлайн · 30 мин · 2 500 ₽                   │
│ [Отменить] [Перенести] [Войти]              │
└─────────────────────────────────────────────┘
```

**AI-консультация (другой стиль):**
```text
┌─────────────────────────────────────────────┐
│ 🤖 8 фев                           [AI]     │
│ Чат с AI-юристом · 12 сообщений             │
│ [Открыть чат]                               │
└─────────────────────────────────────────────┘
```

### Изменения в BottomNav.tsx

Добавить 4-й таб между "Чат" и "Сервисы":

```text
[Лента] [Чат] [История] [Сервисы]
              ↑ новый
```

- Иконка: ClockArrowUp или History
- Подпись: "История"
- Путь: /history

### Изменения в App.tsx

Добавить маршрут:
```
<Route path="/history" element={<History />} />
```

---

## Задача 5: Исправить навигацию

### Проблема
Кнопка X в Chat и Services всегда ведёт на /feed, игнорируя историю браузера.

### Решение

**Chat.tsx - handleClose:**
```text
// Проверить есть ли история
if (window.history.length > 1) {
  navigate(-1);
} else {
  navigate('/feed');
}
```

**Services.tsx:**
Добавить аналогичную кнопку X с той же логикой (если её ещё нет).

---

## Порядок реализации

1. Создать `src/constants/aiAssistants.ts` - конфигурация ассистентов
2. Обновить `Chat.tsx` - универсальный режим с динамическим header
3. Обновить `SuggestionTicker.tsx` - подсказки по сервисам
4. Обновить `ServiceDetail.tsx` - ссылка на чат с сервисом
5. Обновить `ExpertDetail.tsx` - toast + localStorage
6. Создать `src/pages/History.tsx` - страница истории
7. Обновить `BottomNav.tsx` - 4 таба
8. Обновить `App.tsx` - маршрут /history
9. Исправить навигацию в `Chat.tsx`

---

## Технические детали

### localStorage ключи
- `dobro-bookings` - массив записей к экспертам
- `dobro-chat-history` - уже используется для истории чата

### Типы данных

```typescript
interface Booking {
  id: string;
  expertId: string;
  expertName: string;
  specialty: string;
  serviceId: string;
  date: string;
  time: string;
  type: "online" | "chat";
  price: string;
  status: "upcoming" | "completed" | "cancelled";
  createdAt: string;
}

interface AIAssistant {
  id: string;
  name: string;
  icon: LucideIcon;
  color: string;
  suggestions: Suggestion[];
}
```

### Файлы для изменения
- `src/constants/aiAssistants.ts` - **создать**
- `src/pages/History.tsx` - **создать**
- `src/pages/Chat.tsx` - модификация
- `src/components/chat/SuggestionTicker.tsx` - модификация
- `src/pages/ExpertDetail.tsx` - модификация
- `src/pages/ServiceDetail.tsx` - модификация
- `src/components/layout/BottomNav.tsx` - модификация
- `src/App.tsx` - добавить маршрут
