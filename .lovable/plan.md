

# Светлая тема и переключение День/Ночь

## Что будет сделано

Добавим полноценную светлую (дневную) тему и переключатель в стиле iOS/Android — три режима: "Светлая", "Тёмная", "Как на устройстве" (авто).

## Визуальный дизайн переключателя

Переключатель будет в секции "Тема оформления" в настройках — три кнопки в ряд с иконками:

```text
┌──────────┐  ┌──────────┐  ┌──────────┐
│   ☀️     │  │   🌙     │  │   📱     │
│ Светлая  │  │ Тёмная   │  │  Авто    │
└──────────┘  └──────────┘  └──────────┘
```

Активный режим подсвечивается рамкой primary-цвета с галочкой, как сейчас работает выбор tenant-темы. Tenant-цвета (Добросервис/Газпромбанк/Wildberries) остаются ниже отдельным блоком.

## Технические изменения

### 1. `src/index.css` — добавить светлую тему

Добавить блок `.light` с переменными для светлого режима:
- Белый фон (`--background: 0 0% 100%`)
- Тёмный текст (`--foreground: 222 47% 11%`)
- Светлые карточки, popover, muted
- Glass-эффект адаптирован под светлый фон (полупрозрачный белый вместо тёмного)
- Glow-эффекты приглушены для светлого фона
- Tenant-переопределения дублированы для `.light[data-tenant="..."]`

### 2. `src/components/settings/ThemeSwitcher.tsx` — переработать

Разделить на два блока:
- **Режим** (Светлая / Тёмная / Авто) — переключает класс `light`/`dark` на `<html>` и сохраняет в `localStorage`
- **Акцент** (Добросервис / Газпромбанк / Wildberries) — текущий функционал tenant-цветов

Режим "Авто" подписывается на `window.matchMedia('(prefers-color-scheme: dark)')` и реагирует на системные настройки.

### 3. Хранение выбора

Сохранение в `localStorage` под ключом `theme-mode` (`light` | `dark` | `system`). При загрузке приложения — применять до рендера (в `index.html` или в начале `main.tsx`) чтобы избежать мигания.

### 4. `src/main.tsx` — инициализация темы

Добавить скрипт инициализации темы до рендера React, чтобы страница не мигала при загрузке.

## Файлы для изменения

| Файл | Что меняется |
|------|-------------|
| `src/index.css` | Добавить полный набор CSS-переменных для `.light` режима |
| `src/components/settings/ThemeSwitcher.tsx` | Переключатель День/Ночь/Авто + акцент-цвета |
| `src/main.tsx` | Инициализация темы до рендера |
| `index.html` | Инлайн-скрипт для мгновенного применения темы (без мигания) |

