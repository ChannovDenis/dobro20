
# План: Исправление ленты и улучшение чата

## Обзор проблем

1. **Лента не скроллится** — конфликт высоты: `TikTokFeed` занимает `100dvh`, но `BottomNav` перекрывает часть контента, блокируя snap-scroll
2. **Подсказки только про стилиста** — нужны подсказки по разным услугам Добросервиса
3. **Камера рядом с "+"** — нужно переместить камеру в меню под кнопку "+", а само меню сделать компактнее

---

## Часть 1: Исправление ленты

**Проблема:** Контейнер `TikTokFeed` занимает `h-[100dvh]`, а `BottomNav` наложена сверху. Snap-scroll не работает корректно.

**Решение:**

Файл: `src/components/feed/TikTokFeed.tsx`
```text
Было:   h-[100dvh]
Станет: h-[calc(100dvh-80px)] или pb-20
```

Файл: `src/pages/Feed.tsx`
```text
Добавить pb-20 для padding под BottomNav
```

---

## Часть 2: Подсказки по разным услугам

**Текущие подсказки (только стилист):**
- Подбери мне образ
- Определи мой цветотип
- Что модно в 2026?
- Как одеться на собеседование

**Новые подсказки (все услуги Добросервиса):**

```text
⚖️  Юрист       — "Помоги с возвратом товара"
🩺  Врач        — "Какие симптомы у простуды?"
🧠  Психолог    — "Как справиться со стрессом?"
💰  Финансы     — "Как начать копить деньги?"
🛡️  Безопасность — "Как распознать мошенника?"
👗  Стилист     — "Подбери мне образ"
🐕  Ветеринар   — "Чем кормить щенка?"
💪  ЗОЖ         — "Упражнения для утра"
```

**Файл:** `src/pages/Chat.tsx`

Обновим массив `SUGGESTIONS` — 6-8 коротких подсказок по разным экспертам.

---

## Часть 3: Меню "+" с камерой и компактными пунктами

**Текущая структура:**
- Кнопка "+" — открывает модалку с шаблонами
- Кнопка камеры — отдельно в инпуте

**Новая структура:**

Файл: `src/components/chat/ChatInput.tsx`

```text
┌─────────────────────────────────────────┐
│  Кнопка "+"                             │
│  ↓                                      │
│  Модалка:                               │
│  ┌─────────────────────────────────┐    │
│  │ 📷 Загрузить фото               │    │
│  │ ⚖️ Юрист                        │    │
│  │ 🩺 Врач                         │    │
│  │ 🧠 Психолог                     │    │
│  │ 💰 Финансы                      │    │
│  │ 👗 Стилист                      │    │
│  │ 🛡️ Безопасность                 │    │
│  │ 🐕 Ветеринар                    │    │
│  └─────────────────────────────────┘    │
└─────────────────────────────────────────┘
```

**Изменения:**

1. Убрать отдельную кнопку камеры из инпута
2. Добавить "Загрузить фото" первым пунктом в модалке
3. Заменить шаблоны на компактные кнопки экспертов (1 строка на эксперта)
4. Уменьшить padding и отступы — компактный вид

---

## Файлы для изменения

| Файл | Изменения |
|------|-----------|
| `src/components/feed/TikTokFeed.tsx` | Добавить `pb-20` для учёта BottomNav |
| `src/pages/Chat.tsx` | Обновить `SUGGESTIONS` на разные услуги |
| `src/components/chat/ChatInput.tsx` | Убрать кнопку камеры, добавить в меню "+", сделать компактные пункты |

---

## Результат

- Лента будет нормально скроллиться с snap-эффектом
- Подсказки покажут весь спектр экспертов Добросервиса
- Меню "+" станет единой точкой доступа к фото и экспертам
- Интерфейс останется минималистичным и чистым
