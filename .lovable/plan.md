
# План: Оптимизация интерфейса ленты

## Текущее состояние

Сейчас на карточках есть 2 floating-кнопки ("Спросить AI" и "К эксперту"), которые занимают много места. Пользователь хочет сделать интерфейс лаконичнее.

## Что нужно изменить

### 1. Кнопка "Чат" на нижней панели — контекстная навигация

**Текущее поведение:** Кнопка "Чат" просто открывает `/chat`

**Новое поведение:** Если пользователь на странице ленты (`/feed`), кнопка "Чат" передаёт контекст активной карточки:
```
/chat?prompt=...&context=...
```

**Изменения:**
- `BottomNav.tsx` — получает `activeFeedItem` и формирует URL с контекстом
- `TikTokFeed.tsx` — экспортирует текущий activeIndex через callback
- `Feed.tsx` — связывает TikTokFeed и BottomNav, передавая активный item

### 2. Кнопка "К эксперту" — рядом с автором

**Текущее размещение:** Floating-кнопка внизу карточки

**Новое размещение:** Компактная кнопка рядом с именем автора

```
Было:
┌─────────────────────────────┐
│  [Заголовок]                │
│  [Описание]                 │
│                             │
│  👤 Автор имя               │
│                             │
│  [🤖 Спросить AI]          │  ← убрать
│  [📅 К эксперту]           │  ← убрать
└─────────────────────────────┘

Станет:
┌─────────────────────────────┐
│  [Заголовок]                │
│  [Описание]                 │
│                             │
│  👤 Автор имя  [К эксперту] │  ← кнопка рядом с автором
└─────────────────────────────┘
```

---

## Технические детали

### Файлы для изменения

| Файл | Изменения |
|------|-----------|
| `src/components/feed/FeedCard.tsx` | Убрать `FeedCardActions`, добавить кнопку "К эксперту" рядом с автором |
| `src/components/feed/TikTokFeed.tsx` | Добавить callback `onActiveItemChange` |
| `src/pages/Feed.tsx` | Хранить activeItem в state, передавать в BottomNav |
| `src/components/layout/BottomNav.tsx` | Принимать `activeFeedItem` prop, формировать URL с контекстом для кнопки "Чат" |
| `src/components/feed/FeedCardActions.tsx` | Можно удалить (логика переносится) |

### Структура данных

Для передачи контекста в BottomNav нужно:
```typescript
interface ActiveFeedContext {
  title: string;
  tags: string[];
}
```

### Логика маппинга тегов → сервисов

Переносим из `FeedCardActions.tsx` в утилиту или оставляем в `FeedCard.tsx`:
```typescript
const tagToServiceType = {
  "Сад": "garden",
  "Право": "lawyer",
  // ...
};
```

---

## Результат

- Интерфейс становится чище — убираем 2 floating-кнопки
- Кнопка "Чат" на панели работает контекстно — удобнее
- Кнопка "К эксперту" органично вписывается рядом с автором
- Сохраняется вся функциональность
