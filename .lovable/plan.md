
# План: Переместить "Историю" в раздел "Сервисы"

## Суть изменений

Удалить отдельный таб "История" из нижней навигации и добавить доступ к истории консультаций через страницу "Сервисы" — как карточку в разделе быстрых действий или отдельный блок.

---

## Изменения в файлах

### 1. BottomNav.tsx — убрать таб "История"

**Было (4 таба):**
```
[Лента] [Чат] [История] [Сервисы]
```

**Станет (3 таба):**
```
[Лента] [Чат] [Сервисы]
```

- Удалить импорт `Clock`
- Удалить элемент `{ icon: Clock, label: "История", path: "/history" }` из массива `navItems`

---

### 2. Services.tsx — добавить блок "История"

Добавить новую секцию между "AI-ассистент" и категориями сервисов:

```text
┌─────────────────────────────────────────────┐
│ 📋 История консультаций           [→]       │
│ Записи к экспертам и AI-чаты                │
└─────────────────────────────────────────────┘
```

- Импортировать иконку `Clock` из lucide-react
- Добавить `motion.div` карточку с onClick → navigate("/history")
- Стиль аналогичный карточке "AI-ассистент"

---

## Визуальный результат

**Нижняя навигация:**
- Лента (Home) 
- Чат (MessageCircle) — выделенная кнопка
- Сервисы (Grid3X3)

**Страница Сервисы:**
- Промо-карусель
- Быстрые действия (Сканер, Оплата, Кэшбэк, Бонусы)
- AI-ассистент
- **История консультаций** ← новый блок
- Категории сервисов
- Статистика активности

---

## Технические детали

### Файлы для изменения

| Файл | Действие |
|------|----------|
| `src/components/layout/BottomNav.tsx` | Удалить таб "История" |
| `src/pages/Services.tsx` | Добавить карточку "История" |

### Маршрут /history

Маршрут `/history` остаётся в `App.tsx` — страница History.tsx доступна через карточку в Сервисах.
