
# План: Тикер-лента подсказок в AI-чате

## Задача

Переработать блок подсказок в чате: вместо вертикального списка крупных кнопок — создать горизонтально скроллящуюся "биржевую ленту" с компактными чипами в 3 ряда.

## Референс

Пользователь показал референс с компактными тегами разных цветов, расположенными рядами и плавно скроллящимися горизонтально. Нужно сделать похоже, но более сдержанно (не кричаще).

## Новый дизайн

```text
┌─────────────────────────────────────────────────────────────┐
│                        [Bot Icon]                           │
│                     Чем могу помочь?                        │
│                                                             │
│ ← ─────────────────────────────────────────────────────── → │
│  [🌱 Сад] [⚖️ Права] [🩺 Симптомы] [💰 Бюджет] [👗 Стиль]   │ ← Ряд 1 (автоскролл →)
│                                                             │
│ ← ─────────────────────────────────────────────────────── → │
│  [🧠 Стресс] [🐕 Питомцы] [🏋️ Фитнес] [📄 Документы]        │ ← Ряд 2 (автоскролл ←)
│                                                             │
│ ← ─────────────────────────────────────────────────────── → │
│  [🔒 Безопасность] [💼 Работа] [🏠 Дом] [✈️ Путешествия]     │ ← Ряд 3 (автоскролл →)
└─────────────────────────────────────────────────────────────┘
```

## Реализация

### 1. Новый компонент `SuggestionTicker.tsx`

Компонент бегущей ленты с подсказками:

- 3 горизонтальных ряда
- Каждый ряд — скроллящийся контейнер с чипами
- Чётные ряды скроллятся влево, нечётные — вправо (для визуального эффекта)
- Медленная автоматическая анимация (CSS или Framer Motion)
- Можно остановить автоскролл касанием и прокрутить вручную
- Клик по чипу — отправляет промпт в чат

### 2. Расширенный список подсказок

Увеличим количество подсказок до 15-18 штук, чтобы заполнить 3 ряда:

```typescript
const SUGGESTIONS = [
  // Ряд 1
  { icon: "🌱", text: "Что сажать?", prompt: "Что сейчас сажать в моём регионе?" },
  { icon: "⚖️", text: "Возврат товара", prompt: "Помоги с возвратом товара" },
  { icon: "🩺", text: "Симптомы", prompt: "Какие симптомы у простуды?" },
  { icon: "💰", text: "Бюджет", prompt: "Как начать копить деньги?" },
  { icon: "👗", text: "Мой стиль", prompt: "Подбери мне образ" },
  // Ряд 2
  { icon: "🧠", text: "Стресс", prompt: "Как справиться со стрессом?" },
  { icon: "🐕", text: "Питомец", prompt: "Чем кормить щенка?" },
  { icon: "🏋️", text: "Фитнес", prompt: "Упражнения для дома" },
  { icon: "📄", text: "Документы", prompt: "Проверь мой договор" },
  { icon: "💊", text: "Лекарства", prompt: "Аналоги лекарств" },
  // Ряд 3
  { icon: "🔒", text: "Мошенники", prompt: "Как распознать мошенников?" },
  { icon: "💼", text: "Работа", prompt: "Права работника" },
  { icon: "🏠", text: "ЖКХ", prompt: "Как снизить счета ЖКХ?" },
  { icon: "✈️", text: "Отпуск", prompt: "Куда поехать зимой?" },
  { icon: "🎓", text: "Обучение", prompt: "Как выучить новое?" },
];
```

### 3. Стили чипов

- Компактный размер: `px-3 py-1.5 text-xs`
- Glassmorphism фон: `glass` с лёгкой прозрачностью
- Без ярких цветов — приглушённая палитра (серые/muted тона с лёгким акцентом)
- Плавный hover-эффект
- Скруглённые углы: `rounded-full`

### 4. CSS анимация автоскролла

```css
@keyframes ticker-scroll-left {
  0% { transform: translateX(0); }
  100% { transform: translateX(-50%); }
}

@keyframes ticker-scroll-right {
  0% { transform: translateX(-50%); }
  100% { transform: translateX(0); }
}

.ticker-row {
  animation: ticker-scroll-left 30s linear infinite;
}

.ticker-row-reverse {
  animation: ticker-scroll-right 30s linear infinite;
}

.ticker-row:hover,
.ticker-row-reverse:hover {
  animation-play-state: paused;
}
```

### 5. Интеграция в Chat.tsx

Заменить текущий блок `SUGGESTIONS.map()` на новый компонент:

```tsx
// Было:
{SUGGESTIONS.map((suggestion) => (
  <motion.button ...>
    {suggestion.icon} {suggestion.text}
  </motion.button>
))}

// Станет:
<SuggestionTicker onSuggestionClick={handleSuggestionClick} />
```

---

## Технические детали

### Файлы для изменения

| Файл | Изменения |
|------|-----------|
| `src/components/chat/SuggestionTicker.tsx` | Новый компонент тикера |
| `src/pages/Chat.tsx` | Импортировать и использовать `SuggestionTicker` вместо SUGGESTIONS |
| `src/index.css` | Добавить keyframes анимации тикера |

### Структура данных

```typescript
interface Suggestion {
  icon: string;
  label: string;  // Короткий текст для чипа
  prompt: string; // Полный промпт для отправки
}

interface SuggestionTickerProps {
  onSuggestionClick: (prompt: string) => void;
}
```

### Логика автоскролла

Используем дублирование контента для бесшовного скролла:

```tsx
// Дублируем массив для бесшовной анимации
const row1Items = [...row1, ...row1];

<div className="overflow-hidden">
  <div className="flex gap-2 ticker-row">
    {row1Items.map((item, i) => <Chip key={i} {...item} />)}
  </div>
</div>
```

### Остановка при hover/touch

CSS `animation-play-state: paused` при наведении позволяет пользователю остановить ленту и выбрать нужный чип.

---

## Результат

- Компактный, неброский дизайн подсказок
- 3 ряда плавно скроллящихся чипов
- Возможность остановить и выбрать вручную
- Больше подсказок видно сразу
- Современный эффект "биржевого тикера"
